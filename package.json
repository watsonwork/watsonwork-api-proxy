{
  "name": "apiproxy",
  "description": "API proxy for IBM WatsonWork APIs. Allows making API calls with just client_id and client_secret",
  "license": "Apache-2.0",
  "version": "0.0.1",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "semistandard && mocha test.js"
  },
  "devDependencies": {
    "mocha": "^5.2.0",
    "semistandard": "^13.0.1",
    "supertest": "^3.3.0"
  },
  "engines": {
    "node": ">=6.9.1",
    "npm": ">=3.10.8"
  },
  "dependencies": {
    "basic-auth": "^2.0.0",
    "http-proxy": "^1.16.2",
    "jsonwebtoken": "^8.3.0",
    "request": "^2.88.0"
  },
  "semistandard": {
    "env": [
      "node",
      "mocha"
    ]
  }
}
